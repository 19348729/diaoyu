# 03 系统架构

## 3.1 分层架构

```mermaid
graph TB
    Client[微信小程序 / ESP32 / HA]
    Nginx[Nginx 反向代理]
    Gunicorn[Gunicorn + UvicornWorker]
    Server[server.py FastAPI 路由层]
    Domain[domain/ 领域层]
    Infra[infrastructure/ 基础设施层]
    DB[(SQLite / MySQL)]
    QWeather[和风天气 API]
    LBS[腾讯 LBS API]
    LLM[通义千问 DashScope]

    Client --> Nginx
    Nginx --> Gunicorn
    Gunicorn --> Server
    Server --> Domain
    Server --> Infra
    Infra --> DB
    Domain --> QWeather
    Domain --> LBS
    Domain --> LLM
```

层间约束：

- **接入层 → 领域层**：调用 `FishingPredictionService` / `FishingForecastService` / `FishingEngine` / `PrescriptionService` / `QWeatherService` / `TencentLBSService` / `PosterGenerator`。
- **接入层 → 基础设施层**：通过 `Depends(get_db)` 获取 SQLAlchemy `Session`，操作 ORM 模型。
- **领域层 ↛ 基础设施层**：领域层不直接依赖 ORM，只接受 / 返回值对象（`SensorReading`、`SensorTimeSeries`、`ApiData`、`PredictionResult`）。
- **领域层 → 外部 API**：仅通过 `httpx.AsyncClient` 调用，所有调用集中在 [`weather.py`](file:///d:/diaoyu/diaoyu/domain/weather.py)、[`lbs.py`](file:///d:/diaoyu/diaoyu/domain/lbs.py)、[`prescription.py`](file:///d:/diaoyu/diaoyu/domain/prescription.py)。

## 3.2 主预测请求链路

以 `POST /api/predict` 为例：

```mermaid
sequenceDiagram
    participant MP as 小程序
    participant Server as server.py
    participant Weather as QWeatherService
    participant LBS as TencentLBSService
    participant Service as FishingPredictionService
    participant DB

    MP->>Server: POST /api/predict (sensors[], lat, lng, fish_type)
    Server->>Server: Pydantic 校验 PredictRequest
    par 并发拉取上下文
        Server->>Weather: get_realtime_weather(lat,lng)
        Server->>LBS: reverse_geocode(lat,lng)
    end
    Weather-->>Server: ApiData
    LBS-->>Server: location_name
    Server->>Server: _to_reading 兼容 V1/V2，构造 SensorTimeSeries
    Server->>DB: 查询用户数字钓箱
    DB-->>Server: user_inventory dict
    loop 每个鱼种
        Server->>Service: predict_from_series(series, api, inventory)
        Service-->>Server: PredictionResult
    end
    Server->>Server: 按 bite_index 排序，挑选主推荐
    Server->>DB: 保存 PredictionHistory
    Server-->>MP: PredictResponse
```

源码定位：[`server.py` 行 903–1033](file:///d:/diaoyu/diaoyu/server.py)。

## 3.3 核心评分流水线

`predict_from_series` 内部按以下顺序累加修正项（[`services.py` 行 202-419](file:///d:/diaoyu/diaoyu/domain/services.py)）：

```mermaid
graph TB
    A[传感器时序] --> B[构建 SessionContext<br>时段/季节/阶段]
    B --> C[选择参考水温<br>按鱼种水层]
    C --> D[基准分 base_score]
    D --> E[气压动态<br>+一票否决]
    E -->|crash| Z[强制返回 5 分]
    E --> F[溶氧估算+加权]
    F --> G[天气加权]
    G --> H[时段+季节修正]
    H --> I[温跃层 / 三层水温]
    I --> J[温度趋势 / 速率]
    J --> K[月相 Solunar]
    K --> L[短期气压突变]
    L --> M[风向 / 湿度 / 风力]
    M --> N[气压绝对值]
    N --> O[加权混合 + 钳位 0-100]
    O --> P[综合评级 + 战术建议]
```

每一项是否启用受 `report_stage` 决定（见 [01-项目概述 §1.4](./01-项目概述.md#渐进式置信度)）。

## 3.4 V2 AI 救场流程

```mermaid
graph LR
    A[POST /api/v2/rescue] --> B[FishingEngine.evaluate]
    B --> C[输出 metrics+物理 tags]
    C --> D[PrescriptionService]
    D --> E[组装 Prompt<br>system + user]
    E --> F[DashScope qwen-turbo]
    F --> G[解析 JSON 处方]
    G --> H[返回小程序]
```

[`engine.py`](file:///d:/diaoyu/diaoyu/domain/engine.py) 把传感器变率转成结构化 `tags + metrics`，避免让 LLM 做数学计算；[`prescription.py`](file:///d:/diaoyu/diaoyu/domain/prescription.py) 仅负责调用 LLM 并解析 JSON。

## 3.5 数据流总览

```mermaid
graph LR
    ESP32[ESP32 传感器] -->|BLE| MP[小程序]
    MP -->|/api/sensor/realtime<br>/api/sensor/history| Server
    MP -->|/api/predict<br>/api/v2/rescue| Server
    Server -->|SensorRecord| DB[(数据库)]
    Server -->|PredictionHistory| DB
    Server -->|FishingSession| DB
    DB -->|/api/sensor/records| MP
    DB -->|/api/history/logs| MP
    DB -->|/api/session/list| MP
```
